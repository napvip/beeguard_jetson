# BeeGuard Jetson Nano — Dependencies
# Cài trên Jetson Nano sau khi đã cài pycuda (xem HUONG_DAN.md Bước 5).

# Core
# opencv-python  ← DÙNG OpenCV CỦA HỆ THỐNG JETSON (đã bật --system-site-packages)
# numpy          ← DÙNG numpy CÓ SẴN TRÊN HỆ THỐNG JETSON (tránh pip tự compile mất ~1 tiếng)

# AI Inference (TensorRT)
# tensorrt       ← CÓ SẴN trên Jetson (JetPack 4.6), dùng qua venv --system-site-packages
# pycuda         ← pip install pycuda (compile với CUDA 10.2, xem HUONG_DAN.md Bước 5.2)
# Không cần torch / torchvision / ultralytics / onnxruntime trên Jetson — chỉ cần TensorRT + pycuda.

# Hardware
pyserial>=3.5

# Firebase + remote control
python-dotenv>=0.19
requests>=2.25
google-auth>=2.0
