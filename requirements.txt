# BeeGuard Jetson Nano — Dependencies
# Cài trên Jetson Nano sau khi đã cài PyTorch + torchvision từ NVIDIA wheel.

# Core
# opencv-python>=4.1  # DÙNG OpenCV CỦA HỆ THỐNG JETSON (đã bật --system-site-packages)
# numpy>=1.19         # DÙNG numpy CÓ SẴN TRÊN HỆ THỐNG JETSON (tránh pip tự compile từ source mất 1 tiếng)
Pillow>=8.0
pyserial>=3.5

# Deep Learning
# torch        ← CÀI TỪ NVIDIA WHEEL, KHÔNG CÀI TỪ PIP
# torchvision  ← BUILD TỪ SOURCE (xem HUONG_DAN.md)
# ultralytics  ← DÙNG DUAL-MODE OpenCV DNN ONNX (Không cần cài trên Jetson Nano Python 3.6!)

# Firebase + remote control
python-dotenv>=0.19
requests>=2.25
google-auth>=2.0

# QR Code Generation (Cài phiên bản tương thích Python 3.6 - Bản 7.3.1 là bản cao nhất hỗ trợ Python 3.6)
qrcode==7.3.1
